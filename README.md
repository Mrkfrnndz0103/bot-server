# Workflow Server

Workflow server that reads a Google Sheet, keeps only selected columns, and
writes the cleaned data to a destination sheet. Supports polling and
auto-updates when the source changes.

Integration and deployment documentation lives in `docs/`.
