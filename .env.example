PORT=3000
# Provide either inline JSON or a file path to your service account credentials.
# If the file is in the project root, set GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
GOOGLE_SERVICE_ACCOUNT_JSON=
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
POLL_INTERVAL_MS=60000
POLL_JOBS_JSON=[]
POLL_STATE_PATH=
## POLL_JOBS_JSON supports source.gid and destination.gid in place of sheet names.
PING_URL=
PING_INTERVAL_MS=600000
